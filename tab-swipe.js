(()=>{'use strict';function init(){const app=document.querySelector('.app');if(!app||app.dataset.swipeBound==='1')return;app.dataset.swipeBound='1';const order=['inbox','today','calendar','completed','archive'];let sx=0,sy=0,blocked=true;app.addEventListener('touchstart',e=>{if(e.touches.length!==1){blocked=true;return}const t=e.target;blocked=!!t.closest('input,textarea,button,label,a,.note-photos,.list-filters,.toolbar,.quick,.priority-levels,.priority-slider,.photo-viewer,dialog,.brain-dump');sx=e.touches[0].clientX;sy=e.touches[0].clientY},{passive:true});app.addEventListener('touchend',e=>{if(blocked||!e.changedTouches.length)return;const dx=e.changedTouches[0].clientX-sx,dy=e.changedTouches[0].clientY-sy;if(Math.abs(dx)<70||Math.abs(dx)<Math.abs(dy)*1.35)return;const current=document.querySelector('.tab.active')?.dataset.view||'inbox',idx=order.indexOf(current);if(idx<0)return;const next=dx<0?idx+1:idx-1;if(next<0||next>=order.length)return;document.querySelector(`.tab[data-view="${order[next]}"]`)?.click()},{passive:true})}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init):init();})();