(()=>{'use strict';const app=document.querySelector('.app');if(!app)return;let sx=0,sy=0,tracking=false;const isInbox=()=>document.querySelector('#viewTitle')?.textContent.trim()==='인박스'&&!document.querySelector('#normalView')?.classList.contains('hidden');app.addEventListener('touchstart',e=>{if(!isInbox()||e.touches.length!==1||e.target.closest('button,a,input,label,select,textarea,.categories,.share-gallery')){tracking=false;return}sx=e.touches[0].clientX;sy=e.touches[0].clientY;tracking=true},{capture:true,passive:true});app.addEventListener('touchend',e=>{if(!tracking||!e.changedTouches.length)return;tracking=false;const dx=e.changedTouches[0].clientX-sx,dy=e.changedTouches[0].clientY-sy;if(dx<70||Math.abs(dx)<Math.abs(dy)*1.35)return;e.stopImmediatePropagation();document.querySelector('#calendarOpen')?.click()},{capture:true,passive:true})})();