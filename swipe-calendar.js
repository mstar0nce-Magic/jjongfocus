(()=>{'use strict';const app=document.querySelector('.app');if(!app)return;let sx=0,sy=0,mode='';const normalVisible=()=>!document.querySelector('#normalView')?.classList.contains('hidden');const isInbox=()=>normalVisible()&&document.querySelector('#viewTitle')?.textContent.trim()==='인박스';const isCalendar=()=>!document.querySelector('#calendarView')?.classList.contains('hidden');const excluded=e=>e.target.closest('button,a,input,label,select,textarea,.categories,.share-gallery,.card-photo-strip');app.addEventListener('touchstart',e=>{mode='';if(e.touches.length!==1||excluded(e))return;if(isInbox())mode='inbox';else if(isCalendar()&&e.target.closest('#calendarGrid'))mode='calendar-grid';else return;sx=e.touches[0].clientX;sy=e.touches[0].clientY},{capture:true,passive:true});app.addEventListener('touchend',e=>{if(!mode||!e.changedTouches.length)return;const m=mode;mode='';const dx=e.changedTouches[0].clientX-sx,dy=e.changedTouches[0].clientY-sy;if(Math.abs(dx)<70||Math.abs(dx)<Math.abs(dy)*1.35)return;if(m==='inbox'&&dx>0){e.stopImmediatePropagation();document.querySelector('#calendarOpen')?.click();return}if(m==='calendar-grid'){e.stopImmediatePropagation();document.querySelector(dx<0?'#calendarNext':'#calendarPrev')?.click()}},{capture:true,passive:true})})();