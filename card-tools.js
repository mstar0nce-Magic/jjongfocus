(()=>{'use strict';const DB='jjongkeep_simple_v2',STORE='items',CATKEY='damda_categories_v1';let selectMode=false,selected=new Set();const $=s=>document.querySelector(s);function cats(){try{return JSON.parse(localStorage.getItem(CATKEY)||'[]')}catch(e){return[]}}function openDb(){return new Promise((res,rej)=>{const r=indexedDB.open(DB);r.onsuccess=()=>res(r.result);r.onerror=()=>rej(r.error)})}async function getAll(){const db=await openDb();return new Promise((res,rej)=>{const r=db.transaction(STORE).objectStore(STORE).getAll();r.onsuccess=()=>{db.close();res(r.result||[])};r.onerror=()=>rej(r.error)})}async function put(x){const db=await openDb();return new Promise((res,rej)=>{const r=db.transaction(STORE,'readwrite').objectStore(STORE).put(x);r.onsuccess=()=>{db.close();res()};r.onerror=()=>rej(r.error)})}async function del(id){const db=await openDb();return new Promise((res,rej)=>{const r=db.transaction(STORE,'readwrite').objectStore(STORE).delete(id);r.onsuccess=()=>{db.close();res()};r.onerror=()=>rej(r.error)})}function currentView(){const t=($('#viewTitle')?.textContent||'').trim();if(t==='인박스')return'inbox';if(t==='완료')return'completed';const c=cats().find(x=>x.name===t);return c?c.id:'inbox'}async function visibleItems(){const view=currentView();return(await getAll()).filter(x=>view==='completed'?!!x.completed:!x.completed&&(view==='inbox'?!x.categoryId:x.categoryId===view)).sort((a,b)=>(b.createdAt||0)-(a.createdAt||0))}function updateBulk(){const bar=$('#bulkBar');if(!bar)return;bar.classList.toggle('hidden',!selectMode);$('#selectedCount').textContent=selected.size+'개 선택';$('#selectMode').textContent=selectMode?'선택 중':'선택'}async function decorate(){const cards=Array.from(document.querySelectorAll('#list .record'));if(!cards.length)return;const items=await visibleItems();cards.forEach((card,i)=>{const item=items[i];if(!item||card.dataset.tools==='1')return;card.dataset.tools='1';card.dataset.recordId=item.id;const actions=card.querySelector('.card-actions');if(actions){const cat=document.createElement('button');cat.type='button';cat.className='category-quick';cat.textContent='카테고리';cat.onclick=async()=>{const list=cats();const names=['인박스'].concat(list.map(x=>x.name));const now=item.categoryId?(list.find(x=>x.id===item.categoryId)?.name||'인박스'):'인박스';const ans=prompt('카테고리를 입력하세요.\n'+names.join(' / '),now);if(ans===null)return;const name=ans.trim();if(!name||name==='인박스')item.categoryId='';else{const found=list.find(x=>x.name===name);if(!found){alert('등록된 카테고리 이름을 입력해주세요.');return}item.categoryId=found.id}item.updatedAt=Date.now();await put(item);location.reload()};actions.insertBefore(cat,actions.firstChild)}const pick=document.createElement('label');pick.className='card-select';pick.innerHTML='<input type="checkbox" '+(selected.has(item.id)?'checked':'')+'><span>선택</span>';pick.classList.toggle('hidden',!selectMode);pick.querySelector('input').onchange=e=>{e.target.checked?selected.add(item.id):selected.delete(item.id);updateBulk()};card.insertBefore(pick,card.firstChild)})}const observer=new MutationObserver(()=>decorate().catch(console.error));const list=$('#list');if(list)observer.observe(list,{childList:true});$('#selectMode')?.addEventListener('click',()=>{selectMode=!selectMode;if(!selectMode)selected.clear();document.querySelectorAll('.card-select').forEach(x=>x.classList.toggle('hidden',!selectMode));updateBulk()});$('#bulkCancel')?.addEventListener('click',()=>{selectMode=false;selected.clear();document.querySelectorAll('.card-select').forEach(x=>x.classList.add('hidden'));updateBulk()});$('#bulkDelete')?.addEventListener('click',async()=>{if(!selected.size)return;if(!confirm(selected.size+'개의 기록을 삭제할까요?'))return;for(const id of selected)await del(id);selected.clear();selectMode=false;location.reload()});updateBulk();setTimeout(()=>decorate().catch(console.error),300)})();