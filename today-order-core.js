(()=>{'use strict';const DB='jjongkeep_simple_v2',STORE='items';let raf=0,normalizing=false;function isToday(){return document.querySelector('.tab.active')?.dataset.view==='today'}function open(){return new Promise((res,rej)=>{const r=indexedDB.open(DB,1);r.onsuccess=()=>res(r.result);r.onerror=()=>rej(r.error)})}async function normalizeRepeats(){if(normalizing)return;normalizing=true;try{const db=await open();await new Promise((res,rej)=>{const tx=db.transaction(STORE,'readwrite'),store=tx.objectStore(STORE),q=store.getAll();q.onsuccess=()=>{const now=Date.now();for(const i of q.result||[]){if(i.source!=='repeat'||!i.today)continue;const current=Number(i.todayOrder)||0,due=Number(i.repeatDue)||0;if(!current||current===due||current>now+3600000){i.todayOrder=Number(i.createdAt)||now;i.updatedAt=now;store.put(i);const card=document.querySelector(`#list .note[data-item-id="${CSS.escape(String(i.id))}"]`);if(card)card.dataset.todayOrder=String(i.todayOrder)}}};tx.oncomplete=()=>res();tx.onerror=()=>rej(tx.error);tx.onabort=()=>rej(tx.error)});db.close()}catch(e){console.warn('today repeat normalize failed',e)}finally{normalizing=false}}function apply(){raf=0;if(!isToday())return;const list=document.querySelector('#list');if(!list)return;const cards=[...list.querySelectorAll('.note')];const ranked=cards.map((card,index)=>({card,index,set:card.dataset.prioritySet==='1',score:Number(card.dataset.priorityScore)||0,t:Number(card.dataset.todayOrder)||Number(card.dataset.createdAt)||0})).sort((a,b)=>{if(a.set!==b.set)return a.set?1:-1;if(!a.set&&!b.set)return b.t-a.t||b.index-a.index;if(a.score!==b.score)return b.score-a.score;return b.t-a.t||b.index-a.index});ranked.forEach((x,n)=>x.card.style.order=String(n));const summary=list.querySelector('.today-summary');if(summary)summary.style.order='-1'}function clear(){document.querySelectorAll('#list .note').forEach(c=>c.style.removeProperty('order'))}function schedule(){if(raf)return;raf=requestAnimationFrame(apply)}async function sync(normalize=false){if(normalize)await normalizeRepeats();schedule()}function init(){const list=document.querySelector('#list');if(list)new MutationObserver(()=>{if(isToday())schedule()}).observe(list,{childList:true,subtree:false,attributes:true,attributeFilter:['data-priority-set','data-priority-score','data-today-order']});document.addEventListener('click',e=>{const tab=e.target.closest('.tab');if(tab){if(tab.dataset.view==='today')setTimeout(()=>sync(true),0);else setTimeout(clear,0);return}if(isToday()&&e.target.closest('.universal-action,.today-undo,.priority-level'))setTimeout(()=>sync(true),40)},true);document.addEventListener('jjongkeep:datachanged',schedule);window.addEventListener('pageshow',()=>sync(true));sync(true)}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init):init();})();