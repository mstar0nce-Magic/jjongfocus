(()=>{'use strict';function isToday(){return document.querySelector('.tab.active')?.dataset.view==='today'}function reorder(){if(!isToday())return;const list=document.querySelector('#list');if(!list)return;const cards=[...list.querySelectorAll('.note')];if(cards.length<2)return;const sorted=[...cards].sort((a,b)=>{const at=Number(a.dataset.todayOrder)||Number(a.dataset.createdAt)||0,bt=Number(b.dataset.todayOrder)||Number(b.dataset.createdAt)||0;return bt-at});if(cards.every((c,i)=>c===sorted[i]))return;sorted.forEach(c=>list.appendChild(c))}function later(ms=120){setTimeout(reorder,ms)}document.addEventListener('click',e=>{if(e.target.closest('.tab[data-view="today"]'))later(180);if(isToday()&&e.target.closest('.priority-level'))later(120)},true);document.addEventListener('jjongkeep:datachanged',()=>later(120));window.addEventListener('pageshow',()=>later(180));})();