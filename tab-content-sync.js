(()=>{'use strict';let pending=null,old=null,armed=false;function tabs(){return [...document.querySelectorAll('.tab')]}function activate(tab){tabs().forEach(x=>x.classList.toggle('active',x===tab))}function finish(){if(!pending)return;const target=pending;pending=null;old=null;armed=false;activate(target);document.dispatchEvent(new CustomEvent('damda:viewready',{detail:{view:target.dataset.view||''}}))}function start(e){const target=e.target.closest?.('.tab');if(!target||target.classList.contains('active'))return;if(target.dataset.view==='completed'||target.dataset.view==='calendar')return;old=document.querySelector('.tab.active');pending=target;armed=true;queueMicrotask(()=>{if(pending&&old)activate(old)})}function init(){document.addEventListener('click',start,true);const list=document.querySelector('#list');if(!list)return;new MutationObserver(m=>{if(!armed||!pending)return;if(m.some(x=>x.type==='childList'))requestAnimationFrame(finish)}).observe(list,{childList:true});}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init):init();})();