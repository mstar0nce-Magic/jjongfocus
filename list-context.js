(()=>{'use strict';function ensure(){let e=document.querySelector('#activeListContext');if(e)return e;e=document.createElement('div');e.id='activeListContext';e.className='active-list-context hidden';document.querySelector('#listFilters')?.after(e);return e}function paint(){const e=ensure(),active=[...document.querySelectorAll('.list-filter.active')][0];if(!active||!document.querySelector('.tab[data-view="inbox"]')?.classList.contains('active')){e.classList.add('hidden');return}const name=active.textContent.trim();e.innerHTML=`<small>현재 목록</small><strong>${name}</strong>`;e.classList.remove('hidden')}function init(){ensure();const bar=document.querySelector('#listFilters');if(bar)new MutationObserver(()=>requestAnimationFrame(paint)).observe(bar,{childList:true,subtree:true,attributes:true,attributeFilter:['class']});document.addEventListener('click',e=>{if(e.target.closest('.list-filter,.tab'))setTimeout(paint,50)},true);paint()}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init):init();})();