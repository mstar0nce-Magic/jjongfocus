(()=>{'use strict';let token=0;function list(){return document.querySelector('#list')}function lock(){const el=list();if(!el)return;token++;const t=token,h=Math.ceil(el.getBoundingClientRect().height);el.dataset.transitionToken=String(t);if(h>0)el.style.minHeight=h+'px';el.style.visibility='visible';requestAnimationFrame(()=>requestAnimationFrame(()=>{if(el.dataset.transitionToken!==String(t))return;el.style.removeProperty('min-height')}))}function init(){document.querySelectorAll('.tab').forEach(b=>b.addEventListener('pointerdown',lock,true));document.querySelectorAll('.tab').forEach(b=>b.addEventListener('click',lock,true));}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init):init();})();